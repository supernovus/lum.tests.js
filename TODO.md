# TODO

- Write tests for:
  - `call()`
  - `diesWith()`
  - `callIs()`
  - `matches()`
  - `run()`
- Test the `harness/parser` and associated `grammar/tap` libraries.
- Test the *external* test mode in `Harness`.

